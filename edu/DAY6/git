
0. 참고자료 : naver.com search "pro git pdf"
	2번째 블로그 첨부 파일 다운로드 (1~4장 핵심)


1. git 서버 준비
	a. 개인 pc 에 설치
	b. www.github.com에 계정 만들기
		1. 회원가입
		2. repository 생성 - README 파일 생성

2. git client 사용하기
	1) git 설치 - sudo apt-get install git-core
	2) git 환경 설정
		git config --global user.name "이름"
		git config --global user.email "이메일"
		git config --global core.editor vi 		<= git의 commit 작업에 사용할 기본 에디터
	
		설정 확인
		git config --list
		
	3) 서버 프로젝트의 복제본 만들기
		git clone http://github.com/계정명/저장소명
	4) 소스 수정, commit, push
		a. 신규 파일 추가 
			- git status 로 확인

		untracked 
			- 프로젝트에 신규 파일 추가 했을 떄... git add 로 tracked 파일로 변경
			- 프로젝트에 추가 하지 않을 파일은 .gitignore 파일을 만들어서 관리한다.

		tracked 
			-
		* untracked, tracked 개념 알기
		* .gitignore 파일 이해

		modify - 
		stage
		commit
		push'

